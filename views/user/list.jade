table.table.table-striped
  each user in list
    - var name = [user.firstname, user.lastname].join(' ')
    - var isOnline = UserService.isOnline(user)
    tr.user(data-user-id=user.id, class=isOnline ? 'online' : '')
      td
        img(src="/user/avatar/"+ user.id + "?cropped=true", width="60", height="60", alt=name)
      td.vmiddle= name
        span.indicate (online)
      td.vmiddle
        a.request(href="#", data-id=user.id) Добавить в друзья
